I have added .usda parsing (usd stage referencing other usd files) support directly to the viewer.
Stager functionality needs to be combined with it. Stager should be part of the viewer. 
The .usda text should be directly same as what we see in the viewer.
Load an asset reference and allow it to move around the scene. 
Then allow exporting assembly file. Allow user to move rotate and scale assets. Set .usda world scale and y up or Z up whatever
Write clean and optimized rust code. 

Allow loading assets from library to the stage as reference. This should allow users to assembly their assets into .usda and load it into unreal engine for example.
